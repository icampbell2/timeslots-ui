import * as React from 'react';
import './Header.css';

class Header extends React.Component {
  public render() {
    return (
      <header id="header">
        <h1 id="header-title">Welcome to React</h1>
      </header>
    );
  }
}

export default Header;
