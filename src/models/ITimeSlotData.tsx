/**
 * The time slot data model, used to represent user data to be stored for a time slot.
 *
 * @author Ian Campbell
 */
interface ITimeSlotData {
    name: string;
    phoneNumber: string;
}

export default ITimeSlotData;